{
  "name": "speak-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for agent-to-user communication",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "prepare": "npm run build",
    "test": "node --test --experimental-test-coverage dist/**/*.test.js",
    "test:watch": "node --test --watch dist/**/*.test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
